# Maps Directory (Legacy)

**⚠️ This directory is no longer used.**

Maps are now stored in `../../maps/` at the project root.

This ensures proper volume mounting in Docker.
Please edit maps in `./maps` folder.
